import { createTheme } from "@mui/material";

export const theme = createTheme({
    palette: {
        primary: {
            main: '#3f51b5',
            light: '#ff1a8c',
        },
        secondary: {
            main: '#f50057',
        },
        other: {
            main: '#fff',
        }
    }
})